/**
 * Default barrel = the node (main-process) surface. `@kcd` resolves here. The renderer
 * imports the Node-free subset from `@kcd/core` instead — see src/core/index.ts.
 */
export * from './node';
